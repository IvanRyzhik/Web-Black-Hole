# Audio assets

Place optional game audio files here:

- `bgm.mp3` — background music (looped)
- `consume.mp3` — consumption sound effect (one-shot)

The extension works without these files. Once added, enable music and sound effects from the popup.
